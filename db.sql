create table job (
category varchar(20),
cv id int primary key,
options int,
requirement (20)

);

create table your profile(
name v,
qualifilication varchar(20),
category varchar(20),
cv id int,
foreign key f1 (cvid) references job (cvid)

);